// Generated by CoffeeScript 1.3.1
(function() {
  var Auth;

  Auth = (function() {
    var _end_point, _pasword, _token, _username;

    Auth.name = 'Auth';

    _username = '';

    _pasword = '';

    _token = '';

    _end_point = 'https://qiita.com/api/v1/';

    function Auth(username, password) {
      var auth_url;
      auth_url = _end_point + "auth";
      $.ajax({
        url: auth_url,
        type: 'POST',
        data: {
          url_name: username,
          password: password
        },
        dataType: 'json'
      }).done(function(result) {
        return _token = result;
      });
    }

    return Auth;

  })();

}).call(this);
